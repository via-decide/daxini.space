# Alchemist App Mirror

The production GitHub Pages entry remains at the repository root (`index.html`, `MASTER_VAULT.json`, and `sw.js`). This folder is a source/documentation mirror placeholder for the Alchemist Universe application layout and must not become the deployment root until that move is explicitly approved.
